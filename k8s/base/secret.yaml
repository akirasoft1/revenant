apiVersion: v1
kind: Secret
metadata:
  name: discord-article-bot-secrets
  labels:
    app.kubernetes.io/name: discord-article-bot
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # REQUIRED: Discord Bot Token
  # Get this from Discord Developer Portal -> Applications -> Bot -> Token
  DISCORD_TOKEN: "REPLACE_WITH_YOUR_DISCORD_TOKEN"

  # REQUIRED: OpenAI API Key
  # Get this from https://platform.openai.com/api-keys
  OPENAI_API_KEY: "REPLACE_WITH_YOUR_OPENAI_API_KEY"

  # OPTIONAL: MongoDB Password (if using authenticated MongoDB)
  MONGO_PASSWORD: "REPLACE_WITH_YOUR_MONGO_PASSWORD"

  # OPTIONAL: Linkwarden API Token (for self-hosted article archiving)
  # Get this from Linkwarden -> Settings -> Access Tokens
  LINKWARDEN_API_TOKEN: ""

  # OPTIONAL: Gemini API Key (for Google AI features)
  GEMINI_API_KEY: ""

  # OPTIONAL: Ollama API Key (for local LLM inference)
  OLLAMA_API_KEY: ""

  # OPTIONAL: Dynatrace Platform Token (for the agent sidecar's Dynatrace MCP
  # tool access via DT_MCP_URL). Placeholder only — the real token goes into
  # the gitignored k8s/overlays/deployed/secret.yaml.
  DT_PLATFORM_TOKEN: "REPLACE_ME"
#---
# Alternative: Use SealedSecrets or ExternalSecrets for production
# Example ExternalSecrets configuration:
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: discord-article-bot-secrets
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: vault-backend
#   target:
#     name: discord-article-bot-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: DISCORD_TOKEN
#       remoteRef:
#         key: discord-article-bot
#         property: discord-token
#     - secretKey: OPENAI_API_KEY
#       remoteRef:
#         key: discord-article-bot
#         property: openai-api-key
#     - secretKey: MONGO_PASSWORD
#       remoteRef:
#         key: discord-article-bot
#         property: mongo-password
